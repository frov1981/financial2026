<script>
  window.USER_ID = "<%= USER_ID %>"
</script>

<div class="ui-container">
  <h1 class="ui-title">Categorías</h1>

  <!-- Toolbar -->
  <div class="ui-toolbar">

    <!-- Botón Nuevo -->
    <%- include('../../partials/btn-new', { href: '/categories/insert' , title: 'Nueva categoría' , text: 'Nuevo' }) %>

      <!-- Filtrar -->
      <%- include('../../partials/btn-filter', { onclick: 'openFilters()' , title: 'Filtrar categorías' , text: 'Filtrar'
        }) %>

        <!-- Buscador -->
        <%- include('../../partials/search-box', { placeholder: 'Buscar categorías...' }) %>
  </div>

  <!-- Tabla -->
  <div class="ui-table-wrapper">
    <table class="ui-table">
      <thead class="bg-gray-50">
        <tr>
          <th class="ui-th col-left">Nombre</th>
          <th class="ui-th col-left">Tipo</th>
          <th class="ui-th col-left col-sm">Estado</th>
          <th class="ui-th col-left">Acciones</th>
        </tr>
      </thead>
      <tbody id="categories-table" class="bg-white divide-y divide-gray-200"></tbody>
    </table>
  </div>
</div>

<script src="/js/indexes/category-index.js"></script>