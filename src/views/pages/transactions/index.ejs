<script>
  window.USER_ID = "<%= USER_ID %>"
</script>

<div class="max-w-6xl mx-auto">
  <h1 class="ui-title">Transacciones</h1>

  <div class="ui-toolbar">

    <!-- Botón Nuevo -->
    <%- include('../../partials/btn-new', { href: '/transactions/insert' , title: 'Nueva transacción' , text: 'Nuevo' })
      %>

      <!-- Filtrar -->
      <%- include('../../partials/btn-filter', { onclick: 'openFilters()' , title: 'Filtrar transacción' , text: 'Filtrar'
        }) %>

        <!-- Buscador -->
        <%- include('../../partials/search-box', { placeholder: 'Buscar transacciones...' }) %>
  </div>

  <div class="mb-2 flex justify-between items-center text-sm">
    <div id="page-info-top"></div>
    <div class="flex gap-2">
      <button id="prev-page-top" class="ui-btn ui-btn-default">Anterior</button>
      <button id="next-page-top" class="ui-btn ui-btn-default">Siguente</button>
    </div>
  </div>

  <div class="ui-table-wrapper">
    <table class="ui-table">
      <thead class="bg-gray-50">
        <tr>
          <th class="px-4 py-2 text-left text-sm font-medium text-gray-700">Fecha</th>
          <th class="px-4 py-2 text-left text-sm font-medium text-gray-700 ui-col-sm">Tipo</th>
          <th class="px-1 sm:px-2 py-2 text-right text-sm font-medium text-gray-700 whitespace-nowrap">Monto</th>
          <th class="px-4 py-2 text-left text-sm font-medium text-gray-700">Cuenta</th>
          <th class="px-4 py-2 text-left text-sm font-medium text-gray-700">Categoría</th>
          <th class="px-4 py-2 text-left text-sm font-medium text-gray-700">Acciones</th>
        </tr>
      </thead>
      <tbody id="transactions-table" class="bg-white divide-y divide-gray-200"></tbody>
    </table>
  </div>

  <div class="mt-3 flex justify-between items-center text-sm">
    <div id="page-info-bottom"></div>
    <div class="flex gap-2">
      <button id="prev-page-bottom" class="ui-btn ui-btn-default">Anterior</button>
      <button id="next-page-bottom" class="ui-btn ui-btn-default">Siguiente</button>
    </div>
  </div>
</div>

<script src="/js/indexes/transactions-index.js"></script>